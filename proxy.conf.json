{
    "/": {
        "target": "http://localhost:8080",
        "secuire": false
    }
}
// The above code proxies all HTTP requests which start with "/" 
// to http://localhost:8080. Otherwise they use the same